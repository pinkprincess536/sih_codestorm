<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verification Results - Recruiter</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --jharkhand-green: #66cc99;
      --jharkhand-dark-green: #2e8b57;
      --jharkhand-light-green: #d4f0d4;
      --jharkhand-gold: #ffd700;
    }
    body { font-family: 'Segoe UI', sans-serif; background-color: #f8f9fa; min-height: 100vh; display: flex; flex-direction: column; }
    .header { background: linear-gradient(135deg, var(--jharkhand-green), var(--jharkhand-dark-green)); color: white; padding: 12px 0; border-bottom: 5px solid var(--jharkhand-gold); }
    .content-wrapper { flex: 1; display: flex; }
    .sidebar { background: var(--jharkhand-dark-green); min-height: 100%; padding-top: 20px; }
    .sidebar a, .sidebar-btn { display: block; color: white; padding: 12px 20px; text-decoration: none; font-weight: 500; border-left: 4px solid transparent; transition: all 0.3s; background: transparent; border: none; text-align: left; width: 100%; }
    .sidebar a:hover, .sidebar a.active, .sidebar-btn:hover, .sidebar-btn.active { background: var(--jharkhand-green); border-left: 4px solid var(--jharkhand-gold); }
    .main-content { padding: 25px; flex: 1; }
    .footer { background: linear-gradient(135deg, var(--jharkhand-dark-green), var(--jharkhand-green)); color: white; padding: 15px 0; text-align: center; }
  </style>
</head>
<body>
  <div class="header">
    <div class="container-fluid d-flex justify-content-between align-items-center">
      <h4 class="mb-0">CertSure - Recruiter | Verification Results</h4>
      <span>Welcome, <strong>Recruiter</strong></span>
    </div>
  </div>

  <div class="content-wrapper">
    <div class="col-md-3 col-lg-2 sidebar">
      <form method="get">
        <button class="sidebar-btn" type="submit" formaction="/company-dashboard">
          <i class="fas fa-tachometer-alt me-2"></i> Dashboard
        </button>
        <button class="sidebar-btn" type="submit" formaction="/company-upload">
          <i class="fas fa-upload me-2"></i> Upload Certificates
        </button>
        <button class="sidebar-btn active" type="submit" formaction="#">
          <i class="fas fa-check-circle me-2"></i> Verification Results
        </button>
        <button class="sidebar-btn" type="submit" formaction="#">
          <i class="fas fa-history me-2"></i> History
        </button>
        <button class="sidebar-btn" type="submit" formaction="#">
          <i class="fas fa-cog me-2"></i> Settings
        </button> 
      </form>
    </div>

    <div class="col-md-9 col-lg-10 main-content">
      <h3 class="mb-4"><i class="fas fa-check-circle me-2"></i>Verification Results</h3>

      <% if (results && results.length > 0) { %>
        <table class="table table-bordered table-striped">
          <thead class="table-success">
            <tr>
              <th>File Name</th>
              <th>Verification Status</th>
            </tr>
          </thead>
          <tbody>
            <% results.forEach(r => { %>
              <tr>
                <td><%= r.fileName %></td>
                <td>
                  <% if (r.status === 'error') { %>
                    <span class="badge bg-warning text-dark"><%= r.error %></span>
                  <% } else if (r.data?.verification_status?.includes('Authentic')) { %>
                    <span class="badge bg-success"><%= r.data.verification_status.trim() %></span>
                  <% } else { %>
                    <span class="badge bg-danger"><%= r.data?.verification_status || 'Failed' %></span>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } else { %>
        <p class="alert alert-warning">No verification results found.</p>
      <% } %>
    </div>
  </div>

  <div class="footer"></div>
</body>
</html>
